import 'package:flutter/material.dart';

class DetailedStatistics extends StatelessWidget {
  const DetailedStatistics({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Detailed');
  }
}
